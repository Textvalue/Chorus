Brand buttons — blue for primary actions, teal for musical/brand moments, green for success ("on key"). Use for any clickable action.

```jsx
<Button variant="primary">Let's tune up →</Button>
<Button variant="accent" size="lg">Start a Tutti</Button>
<Button variant="secondary">Save draft</Button>
<Button variant="success" iconLeft={<Check/>}>Approve</Button>
<Button variant="ghost" size="sm">Skip</Button>
```

Variants: `primary` (blue) · `accent` (teal) · `success` (green) · `secondary` (outline) · `ghost`. Sizes: `sm` / `md` / `lg`. Props: `fullWidth`, `disabled`, `iconLeft`, `iconRight`.
